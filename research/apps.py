from django.apps import AppConfig


class ResearchConfig(AppConfig):
    """Django app config for the research application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "research"
